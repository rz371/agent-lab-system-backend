[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "'rz371'", email = "z11592764@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi==0.115.13",
  "uvicorn==0.34.0",
  "sqlalchemy==2.0.41",
  "pymysql==1.1.1",
  "pydantic-settings==2.7.1",
  "bcrypt==5.0.0",
  "PyJWT==2.13.0"
]

[project.scripts]
backend = "backend:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
